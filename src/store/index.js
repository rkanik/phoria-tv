import Vue from 'vue'
import Vuex from 'vuex'

// Modules
import Auth from './modules/auth.store'

Vue.use(Vuex)

export default new Vuex.Store({
   state: {
   },
   mutations: {
   },
   actions: {
   },
   modules: {
      Auth
   }
})
